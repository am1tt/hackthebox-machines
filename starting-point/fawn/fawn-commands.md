## 📦 Command Summary (CLI)

```bash
# Nmap Scans
nmap -sC -sV -Pn -n -T4 10.129.X.X
nmap -sV -v -O -sS -p 21 10.129.X.X

# FTP Connection
ftp 10.129.X.X
# login: anonymous
# password: [press Enter]

# FTP Commands
ls
get flag.txt
quit

# Read Flag Locally
cat flag.txt
```
